[pytest]
# Suppress Qt event loop exception catching to avoid failures on napari cleanup
qt_no_opengl = true
qt_api = pyqt5
addopts = -v --tb=short

# Mark napari tests
markers =
    napari: mark test as using napari viewer
    gui: marks tests as GUI tests (deselect with '-m "not gui"')



    